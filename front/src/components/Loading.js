import React from 'react';
import Layout from '../components/Layout';

function Loading() {
    return (
        <Layout>
            <h1>Loading...</h1>
        </Layout>
    );
}

export default Loading;
